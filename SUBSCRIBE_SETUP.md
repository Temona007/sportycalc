# Email Subscribe Popup Setup

The email capture popup sends subscriptions to **temona007@gmail.com** via Formspree.

**Current config (Vercel):** Uses Formspree endpoint `https://formspree.io/f/xkoqbwvp` — no backend needed.

To change the endpoint, set `window.SPORTYCALC_SUBSCRIBE_URL` before the email-popup script loads.
